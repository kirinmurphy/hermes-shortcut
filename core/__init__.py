"""core — the execution code for the shortcut plugin.

Flows (macros over native hermes commands), utilities (read-only), and the
plumbing they share (argparse tree, records bookkeeping, subprocess runner).
"""
